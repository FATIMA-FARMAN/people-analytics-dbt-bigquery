# People Analytics dbt (BigQuery) + Airflow Orchestration

## What this repo shows
- dbt project targeting BigQuery (models + tests)
- Airflow DAG triggering dbt runs (local execution)

## Proof
Screenshots will be added in `docs/screenshots/` and referenced here.

## Repo structure
- `dags/` Airflow DAGs
- `models/` dbt models
- `tests/` dbt tests
